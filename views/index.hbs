<div id="cardsBody">
  {{#each card}}
  <form data-id="{{this.id}}" class="cards"><a href="/card/{{this.id}}">
    <div data-id="{{this.id}}" class="word">{{this.name}} {{this.id}}</div>
    <img class="cardsImage" src="{{this.img}}"></a>
    <div class="countPost">Количество: {{this.Posts.length}}</div>
  </form>
  {{/each}}
     <div class="round"><a href="#" class="buttonUp">⬆</a></div>
</div>

